# Local YouTrack instance from Docker. Copy this file to .env, which is gitignored.
YT_BASE_URL=http://localhost:8080
# Permanent token from the admin user's profile. Current YouTrack versions issue it
# in the form perm-... - use that value as it is, without a prefix.
YT_TOKEN=REPLACE_ME
